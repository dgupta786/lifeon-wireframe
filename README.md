# LifeON wireframe

Review copy of the LifeON market-study wireframe (SCG A Builders | Project Build).
Single self-contained page: `index.html`. Published with GitHub Pages.

The working copy lives in the main project folder under `wireframe/`; this repo only mirrors it for sharing.
